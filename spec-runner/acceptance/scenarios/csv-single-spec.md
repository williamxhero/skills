# Run-marked single SPEC acceptance brief

In the assigned `spec-runner/acceptance/fixture-app/<run-marker>/` directory,
create a Python command that reads a task CSV with columns `id`, `title`, and
`status`, validates rows and emits deterministic JSON. Invalid input must fail
without writing a partial result. Add tests. The Runner owns planning,
implementation, independent review and delivery.
