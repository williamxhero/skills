# Acceptance code scope

Each run has a unique child directory here. Only the acceptance worker for
that run may create or modify code inside its assigned child directory.
No example implementation or preapproved review is supplied.
