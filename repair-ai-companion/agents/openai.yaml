interface:
  display_name: "修复AI伴生"
  short_description: "持续跨工程修复、真实重测并沉淀可复用经验"
  default_prompt: "Use $repair-ai-companion to repair the named AI companion request until a real end-to-end reply passes, while capturing verified reusable lessons in an independent research-process document."
policy:
  allow_implicit_invocation: true
