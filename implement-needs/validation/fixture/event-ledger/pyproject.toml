[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qualification-event-ledger"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
event-ledger = "event_ledger.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
