## Parent

Part of #54 (SPEC P1-6: 真正实现阶段最小上下文与新鲜度保护)

## What to build

让上下文驱动的状态修改携带并校验投影读取版本；上下文产生后如果业务状态已变化，写入必须返回结构化 stale_state 和刷新指令，不能继续执行旧上下文中的副作用。

## Acceptance criteria

- 使用投影 version 的状态修改在版本不一致时原子拒绝，业务状态和事件均不变化。
- CLI/API 的成功写入和 stale_state 拒绝均有稳定结构化结果。
- 重试必须先重新读取投影，不能通过缺省或空 expected version 绕过该闸门。
- 既有 P0/P1 状态 gate、授权、Intent 和恢复语义保持不变。

## Blocked by

- #85
