## Parent

Part of #53 (SPEC P1-5: 固定策略、迁移、备份恢复与外部对账)

## What to build

提供 policy/backup/recovery CLI 和重启回归，证明策略 drift、损坏 manifest、外部动作未对账不能继续副作用。

## Acceptance criteria

- CLI/API 可 pin、迁移、生成/验证 manifest、进入 restore pending 和记录 post-backup reconciliation。
- 恢复前检查结果结构化且 fail closed。
- 重启后 policy identity、manifest、未决 Intent 与对账状态可重建。

## Blocked by

- P1-5.2 backup/restore validation