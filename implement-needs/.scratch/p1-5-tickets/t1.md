## Parent

Part of #53 (SPEC P1-5: 固定策略、迁移、备份恢复与外部对账)

## What to build

为 run 建立不可变 policy identity：同内容重复 pin 幂等，不同内容必须显式迁移，执行/ shadow 加载时验证摘要一致。

## Acceptance criteria

- policy canonical metadata、content digest、implementation digest 持久化。
- 相同 pin 不产生语义变化；不同 pin 默认 fail closed。
- 显式迁移必须带 compatibility、authorization、rollback evidence。
- loaded implementation digest 不匹配时执行和 shadow 都拒绝。

## Blocked by

- None (can start immediately)