## Parent

Part of #55 (SPEC P2-1: 以单一动作契约生成文档、CLI 与检查表)

## What to build

从已验证的动作契约生成 CLI 帮助、阶段检查表和 dispatch 指引，使输入、gate、允许副作用和恢复语义不再由分散文字重复定义。

## Acceptance criteria

- 生成物明确标记为 derived，并显示动作输入、证据、恢复和参考。
- dispatch 不再对未知动作返回泛化执行指引。
- CLI/阶段表与同一契约版本一致。

## Blocked by

- #88
