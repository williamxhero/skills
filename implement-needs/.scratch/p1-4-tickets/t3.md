## Parent

Part of #48 (SPEC P1-4: 持久化线程准备与测试列车检查点)

## What to build

公开 bootstrap/checkpoint CLI，接入 reconcile，完成异步延迟、归档、checkpoint 阻塞的回归测试。

## Acceptance criteria

- CLI/API 可记录 route readback、assignment、取消、归档和 checkpoint 结果。
- reconcile/next_action 输出唯一待处理 bootstrap 或 checkpoint 动作。
- 重启后可恢复同一线程与测试列车，不创建重复 owner。

## Blocked by

- P1-4.2 测试列车检查点