## Parent

Part of #56 (SPEC P2-2: 以行为安全指标和覆盖率评价运行效率)

## What to build

定义固定 benchmark task set 与独立的 correctness/observability 数据模型，明确成功交付、错误完成、恢复、重复副作用、Token、费用和 coverage 的语义；simulation 与真实闭环使用不同标签。

## Acceptance criteria

- benchmark 任务集固定、可重放且不改变完成 gate。
- 缺失数据为 unknown 并带 coverage，不被转换为零或成功。
- correctness 断言与 telemetry 字段结构分离。

## Blocked by

None (can start immediately)
