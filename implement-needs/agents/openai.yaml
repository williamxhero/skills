interface:
  display_name: "Implement Needs"
  short_description: "Delegate planning, implementation, tests, and release"
  default_prompt: "Use $implement-needs to delegate transparent Chinese Grill, SPEC/ticket planning, and implementation routing to one high-reasoning task, auto-accept its defaults, verify every applied route, then supervise delivery and release."
