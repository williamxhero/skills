interface:
  display_name: "Implement Needs"
  short_description: "Route specs, blockers, and release-train tests"
  default_prompt: "Use $implement-needs to default-grill this requirement, publish and auto-approve each scoped SPEC, deliver it in its own task, test through $test-release-train, repair blockers, then build and deploy."
