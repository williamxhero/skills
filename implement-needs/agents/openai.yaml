interface:
  display_name: "Implement Needs"
  short_description: "Delegate planning, implementation, tests, and release"
  default_prompt: "Use $implement-needs to show and auto-accept a Chinese default Grill, delegate SPEC/ticket planning and implementation routing to one high-reasoning task, supervise each spec in its own task, repair blockers, test, build, and deploy."
